name = "iamraf"
compatibility_date = "2026-08-30"

# Static CV via Workers Assets - hanya public (biar node_modules gak 97MiB)
assets = { directory = "./public", not_found_handling = "single-page-application" }
